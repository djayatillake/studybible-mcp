# Core dependencies
mcp>=1.0.0
httpx>=0.25.0
aiosqlite>=0.19.0
pydantic>=2.0.0
click>=8.0.0
rich>=13.0.0

# SSE transport dependencies (included for Docker builds)
starlette>=0.32.0
uvicorn>=0.24.0

# Vector search dependencies
sqlite-vec>=0.1.0

# Embedding generation (optional): pip install studybible-mcp[embeddings]
# openai>=1.0.0
