# Core dependencies
# Pinned: the 2.x SDK removed Server.list_tools(), which crash-loops this server
# on startup. Verified working against 1.26.0. Unpin only alongside a migration
# of the @server.list_tools()/@server.call_tool() decorators in server.py.
mcp==1.26.0
httpx>=0.25.0
aiosqlite>=0.19.0
pydantic>=2.0.0
click>=8.0.0
rich>=13.0.0

# SSE / Streamable HTTP transport dependencies (included for Docker builds)
starlette>=0.32.0
uvicorn>=0.24.0
sse-starlette>=1.6.0

# Vector search dependencies
sqlite-vec>=0.1.0

# Embedding generation (optional): pip install studybible-mcp[embeddings]
# openai>=1.0.0
